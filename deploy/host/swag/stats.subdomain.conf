## SWAG proxy-conf for stats.avalia.rocks → Umami on the mini-pc.
## Drop into /mnt/user/appdata/swag/nginx/proxy-confs/ on Unraid,
## then `docker restart swag`.

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;

    server_name stats.*;

    include /config/nginx/ssl.conf;

    client_max_body_size 0;

    # Umami runs on the mini-pc (different host than SWAG), so we proxy
    # over the LAN IP directly rather than docker DNS.
    location / {
        include /config/nginx/proxy.conf;
        include /config/nginx/resolver.conf;
        set $upstream_app 192.168.178.29;
        set $upstream_port 8088;
        set $upstream_proto http;
        proxy_pass $upstream_proto://$upstream_app:$upstream_port;
    }
}
